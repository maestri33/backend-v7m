interface:
  display_name: "Backend Supletivo"
  short_description: "Repo-specific patterns and workflows for backend-supletivo"
  default_prompt: "Use the backend-supletivo repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true